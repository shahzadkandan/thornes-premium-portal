export default function ProductsPage() {
  return (
    <section className="min-h-screen bg-white px-6 py-20 text-[color:var(--navy)]">
      <div className="mx-auto max-w-5xl">
        <p className="text-sm font-bold uppercase tracking-[0.2em] text-[color:var(--teal-dark)]">
          Products
        </p>
        <h1 className="mt-4 font-display text-5xl">Sourcing catalogue placeholder.</h1>
        <p className="mt-6 text-xl text-slate-600">
          Product categories will be populated from Headless WordPress after manufacturer or
          supplier attribution and document availability are verified.
        </p>
      </div>
    </section>
  );
}
